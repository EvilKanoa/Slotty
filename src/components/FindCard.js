import React from 'react';

import Card from './Card';

const FindCard = () => (
  <Card header="Look up an existing notification">
    This feature is currently under construction. It should be available shortly, check
    back soon! If you absolutely need to look up a notification right now, then check out
    the API docs so you can do it manually.
  </Card>
);

export default FindCard;
